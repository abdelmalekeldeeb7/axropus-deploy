# Korith-Poly requirements
# Minimal dependencies — all ML is built from scratch (no sklearn/torch needed)

# Polymarket CLOB client
py-clob-client>=0.17.0

# HTTP + WebSocket
requests>=2.31.0
websocket-client>=1.6.0

# Crypto trading
python-binance>=1.0.19

# Config / env
python-dotenv>=1.0.0

# Optional: vLLM for local AMF inference (install separately on GPU machine)
# vllm>=0.4.0

# Optional: Ollama Python client
# ollama>=0.1.0
