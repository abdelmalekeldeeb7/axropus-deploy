apiVersion: v1
kind: Service
metadata:
  name: korith-gateway
spec:
  selector:
    app: korith-gateway
  ports:
    - protocol: TCP
      port: {{ .Values.gateway.port }}
      targetPort: {{ .Values.gateway.port }}
